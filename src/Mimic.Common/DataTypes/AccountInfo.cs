namespace Mimic.Common
{
    public class AccountInfo
    {
        public int id, failed_logins, expansion, locale;
        public bool locked, online;
        public string username, pass_hash, sessionkey, v, s, token_key, email, join_date, last_ip, lock_country, last_login, os;
    }
}
